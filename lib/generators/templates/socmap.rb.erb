SocmapAdf.setup do |config|

  # == You can specify which adf assets to use
  #
  #   config.assets = ["mvc", "map", "minimap", "gmap", "overlay", "overlay_push", "marker", "zone", "sidebar", "form", "popup", "image_uploader", "cluster", "file_uploader"]
  #
  # By default it use all assets
  
end